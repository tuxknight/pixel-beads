# Beads — 拼豆图纸设计工具

## 产品定位

跨平台拼豆（Perler Beads / Fuse Beads）图纸设计工具，支持微信小程序 + Android App + iOS App + 网页访问。

## 目标用户

- 拼豆爱好者（中轻度用户为主）
- 需求：上传图片 → 自动转图纸 → 打印/手机上看 → 买豆子照着拼

## MVP 功能（版本 0.1）

核心功能只有 4 个，不贪多：

1. **画布编辑器** — 像素网格，点格子填色，选品牌色板
2. **图片导入转图纸** — 上传图片，自动像素化 + 映射到品牌色号，显示色号清单
3. **导出图纸** — PNG 图片 + 颜色用量清单（什么颜色买几颗）
4. **图纸保存** — 本地 localStorage，不要求登录

### 非 MVP（以后再做）
- 用户系统
- 社区分享/发现图纸
- 图层/临摹/文字工具
- 3D 预览
- 拍照识别色号

## 技术方案

基于 Jett-Wu/Perler_Beads_Generator 的架构思路，但用 Next.js 重新实现以获得多端 WebView 嵌入能力。

### 技术栈
- **框架**: Next.js + TypeScript
- **画布**: react-konva (基于 Konva.js)
- **样式**: Tailwind CSS
- **App 套壳**: Capacitor (后续)
- **小程序**: WebView 组件嵌入 (后续)
- **部署**: Cloudflare Pages / GitHub Pages

### 项目结构
```
beads/
├── src/
│   ├── app/              # Next.js App Router
│   │   ├── layout.tsx
│   │   ├── page.tsx      # 主页面（画布编辑器）
│   │   └── globals.css
│   ├── lib/
│   │   ├── types.ts      # 类型定义
│   │   ├── palettes.ts   # 色板数据 + 颜色映射算法
│   │   └── storage.ts    # localStorage 存/取/删
│   ├── components/
│   │   ├── Canvas.tsx     # 主画布（Konva）
│   │   ├── Toolbar.tsx    # 工具栏
│   │   ├── PalettePanel.tsx  # 色板面板
│   │   ├── ImageImport.tsx   # 图片导入对话框
│   │   └── ExportDialog.tsx  # 导出对话框
│   └── hooks/
│       └── useCanvas.ts  # 画布状态管理
```

## 设计规范

### 画布
- 默认尺寸 32x32 格子
- 每格渲染为一个 Konva Rect
- 缩放/平移：鼠标滚轮缩放，拖拽平移
- 点击填充颜色，右键擦除
- 网格线显示（浅灰色）

### 色板
- 顶部显示三个品牌切换（Perler / Artkal / Hama）
- 颜色以圆形色块排列，可滚动
- 当前选中颜色高亮显示
- 最近使用的颜色置顶

### 导入图片
- 支持 PNG/JPG/WebP
- 调整目标尺寸（默认保持宽高比）
- 色数上限（减少颜色种类）
- 映射到品牌色板的最接近色
- 预览效果 + 确认/取消

### 导出
- PNG 图纸：网格 + 颜色色块编号
- 颜色清单：按颜色分组显示用量（颗数）

### 页面布局
```
┌─────────────────────────────────────┐
│  标题栏   色号品牌选择   导出 保存    │
├─────────┬───────────────────────────┤
│ 工具栏  │                           │
│ ┌─────┐ │      画布（Konva）        │
│ │画笔  │ │                           │
│ │橡皮  │ │  可缩放 + 可拖拽          │
│ │填充  │ │                           │
│ │吸管  │ │                           │
│ └─────┘ │                           │
│         │                           │
│ 色板    │                           │
│ ┌──┐   │                           │
│ │○ ○│  │                           │
│ │○ ○│  │                           │
│ └──┘   │                           │
│         │                           │
├─────────┴───────────────────────────┤
│  状态栏: 网格尺寸 | 颜色数 | 总颗数  │
└─────────────────────────────────────┘
```
